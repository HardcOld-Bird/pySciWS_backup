# ===== 缓存与临时文件（默认绝不入库）=====
# cache 下的一切默认忽略（PDF/html/api_responses 等大文件或易失产物），
# 唯独 cache/extracted/ 例外——它是 MinerU 配额换来的转换全文（.md），
# 重获代价高，故纳入 Git 跟踪做备份（与 skill 的 read 缓存复用、cache 治理完全兼容）。
cache/*
!cache/extracted/
# COMSOL 手册的 MinerU 原始缓存：comsol_simulation 的 docs 转换管线借道本 pdf_extract 产生，
# 属可再生的大文件（docs 全文亦按大文件策略忽略），此处原始件同样不入库（须置于上面的
# !cache/extracted/ 之后，才能覆盖该反忽略规则）。
cache/extracted/COMSOL_*
cache/extracted/AcousticsModuleUsersGuide_*
cache/extracted/ApplicationProgrammingGuide_*
*.tmp
*.log

# ===== 原始文件（PDF/EPUB 由 Zotero 管理，不入 Git）=====
*.pdf
*.epub
*.zip
*.tar
*.tar.gz

# ===== 凭据 =====
.env
*.key
*.pem

# ===== Python 缓存 =====
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
