[project]
name = "pysci"
version = "0.1.0"
description = "N/A"
readme = "README.md"
# 锁定 Python 精确版本（与项目根目录 .python-version 保持一致）
# uv sync 会自动下载/使用该解释器
requires-python = "==3.13.15"
license = {text = "MIT"}
authors = [
    {name = "HardcOld-Bird", email = "antarcticbird@163.com"}
]

# ------------------------------------------------------------
# 运行时依赖（由 environment.yml 迁移而来）
# ------------------------------------------------------------
dependencies = [
    # 科学计算核心
    "numpy",
    "scipy",
    "sympy",
    # 绘图
    "matplotlib",
    "scienceplots",
    "ultraplot",
    "pyvista",
    # 数据处理 / 校验
    "pandas",
    "pydantic",
    # COMSOL 仿真接口
    "mph",
    # 文献检索与知识库（skills_src/literature_research/ 目录下的工具脚本使用，详见其 README.md）
    "requests",         # HTTP 客户端（OpenAlex / arXiv / WoS / Zotero）
    "python-dotenv",    # 加载项目根 .env
    "pyzotero",         # Zotero API 官方 Python 封装
    "markdown",         # markdown → HTML（Zotero 笔记写入时需要）
    # PDF → Markdown 抽取（skills_src/literature_research/tools/pdf_extract.py 使用）
    # 公式精读首选云端 MinerU Open API（仅需 requests，无本地重型依赖）；
    # marker-pdf / magic-pdf 实测本机不可用且云端可替代，已移除。
    "pymupdf4llm",      # 本地兜底后端：纯 CPU，快，但公式会丢失
    "playwright>=1.63.0",  # 付费墙论文抓取：HTML 全文 / 正文 PDF / 补充材料
]

# ------------------------------------------------------------
# 开发依赖组（PEP 735）：uv sync 默认会安装，无需 --extra
# ------------------------------------------------------------
[dependency-groups]
dev = [
    "ruff",
    "pytest",
    "pytest-cov",
    "pre-commit",
    "commitizen",
]

[tool.uv]
# 本项目是脚本/应用型工程：代码通过 `from src...` / `from config...` 直接导入，
# 从不以 `import pysci` 方式使用，因此关闭打包。
# uv 只负责创建 .venv、解析并安装依赖，不会尝试构建/安装 pysci 本身。
package = false

# 默认索引指向清华 TUNA 镜像，加速国内下载（如需换回官方源，删除 default = true 即可）
[[tool.uv.index]]
name = "tuna"
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
default = true

[project.urls]
Homepage = "https://github.com/HardcOld-Bird/pySciWS_backup"
Repository = "https://github.com/HardcOld-Bird/pySciWS_backup.git"
Issues = "https://github.com/HardcOld-Bird/pySciWS_backup/issues"

[tool.ruff]
target-version = "py313"
line-length = 88
exclude = [
    ".git/**",
    ".idea/**",
#    "scripts/**",
]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort (import sorting)
    "N",      # pep8-naming
    "UP",     # pyupgrade
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "SIM",    # flake8-simplify
]
ignore = [
    "E501",   # Line too long (handled by formatter)
]

[tool.ruff.lint.isort]
# Configure import sorting
known-first-party = ["src"]

[tool.commitizen]
name = "cz_conventional_commits"
tag_format = "0.1.0"
version_scheme = "pep440"
version_provider = "pep621"
update_changelog_on_bump = true
major_version_zero = true
